import { HoldButtonDirective } from './hold-button.directive';

describe('HoldButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new HoldButtonDirective();
    expect(directive).toBeTruthy();
  });
});
